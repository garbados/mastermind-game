{
  "name": "mastermind",
  "version": "0.0.0",
  "description": "A mastermind CLI and solver AI",
  "main": "index.js",
  "scripts": {
    "jshint": "./node_modules/.bin/jshint bin lib test",
    "mocha": "./node_modules/.bin/mocha -R spec --recursive",
    "cov": "./node_modules/.bin/jscoverage lib cov",
    "report": "./node_modules/.bin/mocha NODE_COVERAGE=true -R html-cov > index.html",
    "test": "npm run jshint && npm run mocha && npm run cov && npm run report"
  },
  "repository": {
  	"type": "git",
  	"url": "http://github.com/garbados/mastermind.js.git"
  },
  "author": "Diana Thayer <me@dianathayer.org>",
  "license": "ISC",
  "devDependencies": {
    "chai": "^1.10.0",
    "jshint": "^2.5.11",
    "mocha": "^2.1.0"
  },
  "dependencies": {
    "async": "^0.9.0",
    "prompt": "^0.2.14",
    "yargs": "^1.3.3"
  }
}
